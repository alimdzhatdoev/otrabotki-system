# 🚀 Инструкция по деплою на Render

Это руководство поможет вам развернуть приложение "Система Отработки" на Render.

## 📋 Подготовка

### 1. Убедитесь, что проект готов к деплою

- Все зависимости установлены
- Проект работает локально
- Все файлы закоммичены в Git

### 2. Создайте репозиторий на GitHub (если еще нет)

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <ваш-репозиторий-url>
git push -u origin main
```

## 🔧 Деплой Backend (API)

### Шаг 1: Создайте новый Web Service на Render

1. Зайдите на [render.com](https://render.com) и войдите в аккаунт
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий `otrabotki-system`

### Шаг 2: Настройте Backend Service

**Основные настройки:**
- **Name:** `otrabotki-backend` (или любое другое имя)
- **Environment:** `Node`
- **Build Command:** `cd server && npm install`
- **Start Command:** `cd server && npm start`
- **Plan:** `Free` (или выберите платный план)

**Переменные окружения (Environment Variables):**
Добавьте следующие переменные:

```
NODE_ENV=production
PORT=10000
JWT_SECRET=<сгенерируйте-надежный-секретный-ключ>
FRONTEND_URL=<url-вашего-фронтенда>
```

**Важно:**
- `JWT_SECRET` - сгенерируйте надежный случайный ключ (можно использовать онлайн-генератор)
- `FRONTEND_URL` - пока оставьте пустым, добавите после деплоя фронтенда
- `PORT` - Render автоматически устанавливает порт через переменную `PORT`, но можно указать явно

### Шаг 3: Сохраните и деплойте

Нажмите **"Create Web Service"**. Render начнет деплой.

**После успешного деплоя:**
- Запишите URL вашего бэкенда (например: `https://otrabotki-backend.onrender.com`)
- Проверьте работу API: `https://otrabotki-backend.onrender.com/api/health`

## 🎨 Деплой Frontend (Static Site)

### Шаг 1: Создайте новый Static Site на Render

1. Нажмите **"New +"** → **"Static Site"**
2. Подключите тот же GitHub репозиторий

### Шаг 2: Настройте Frontend Service

**Основные настройки:**
- **Name:** `otrabotki-frontend`
- **Build Command:** `npm install && npm run build`
- **Publish Directory:** `dist`

**Переменные окружения:**
⚠️ **КРИТИЧЕСКИ ВАЖНО:** Добавьте переменную `VITE_API_URL` (НЕ `FRONTEND_URL`!):

```
VITE_API_URL=https://otrabotki-system-back.onrender.com/api
```

**Важно:** 
- Замените `otrabotki-system-back.onrender.com` на реальный URL вашего бэкенда!
- Переменная должна называться именно `VITE_API_URL` (с префиксом `VITE_`), иначе Vite не подхватит её при сборке!
- Если вы случайно добавили `FRONTEND_URL` на фронтенде - УДАЛИТЕ её, она нужна только на бэкенде!

**Настройка роутинга (важно!):**
Для корректной работы React Router при обновлении страницы нужно настроить redirects.

**Вариант 1: Через файл `_redirects` (рекомендуется)**
Файл `public/_redirects` уже создан в проекте и будет автоматически скопирован в `dist` при сборке. Он содержит правило:
```
/*    /index.html   200
```

**Вариант 2: Через Render Dashboard**
Если файл не работает, настройте вручную:
1. В настройках Static Site найдите раздел **"Redirects/Rewrites"**
2. Добавьте правило:
   - **Source:** `/*`
   - **Destination:** `/index.html`
   - **Status Code:** `200`

Это правило говорит серверу: "Все запросы к несуществующим путям возвращай `index.html` со статусом 200, чтобы React Router мог обработать маршрут на клиенте".

### Шаг 3: Обновите FRONTEND_URL в Backend

После деплоя фронтенда:
1. Запишите URL фронтенда (например: `https://otrabotki-frontend.onrender.com`)
2. Вернитесь в настройки Backend Service
3. Обновите переменную `FRONTEND_URL`:
   ```
   FRONTEND_URL=https://otrabotki-frontend.onrender.com
   ```
4. Нажмите **"Save Changes"** - Render автоматически перезапустит сервис

## ✅ Проверка работы

1. Откройте URL фронтенда в браузере
2. Попробуйте войти с тестовыми учетными данными
3. Проверьте, что все функции работают

## 🔄 Автоматический деплой

Render автоматически деплоит при каждом push в основную ветку (обычно `main` или `master`).

## 📝 Важные замечания

### Free Plan ограничения

На бесплатном плане Render:
- Сервисы "засыпают" после 15 минут неактивности
- Первый запрос после "сна" может занять 30-60 секунд (cold start)
- Для production рекомендуется использовать платный план

### Хранение данных

⚠️ **Важно:** JSON файлы хранятся в файловой системе сервера. При перезапуске или пересоздании сервиса данные могут быть потеряны!

**Рекомендации для production:**
- Используйте внешнюю базу данных (PostgreSQL, MongoDB)
- Или используйте Render Disk для постоянного хранения файлов
- Или настройте автоматическое резервное копирование

### CORS настройки

Если возникают проблемы с CORS:
1. Убедитесь, что `FRONTEND_URL` в бэкенде указан правильно
2. Проверьте, что URL фронтенда точно совпадает (включая `https://`)

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте логи в Render Dashboard
2. Убедитесь, что все зависимости установлены
3. Проверьте переменные окружения

### Frontend не подключается к Backend (ERR_CONNECTION_REFUSED к localhost:5000)

**Симптомы:** В консоли браузера ошибка `localhost:5000/api/auth/login:1 Failed to load resource: net::ERR_CONNECTION_REFUSED`

**Причина:** Фронтенд пытается подключиться к локальному серверу вместо бэкенда на Render.

**Решение:**
1. ✅ Проверьте, что на фронтенде установлена переменная **`VITE_API_URL`** (НЕ `FRONTEND_URL`!)
2. ✅ Убедитесь, что значение правильное: `https://ваш-бэкенд.onrender.com/api`
3. ✅ **Удалите** переменную `FRONTEND_URL` с фронтенда, если она там есть (она нужна только на бэкенде!)
4. ✅ После изменения переменных окружения **пересоберите и передеплойте** фронтенд (Render должен сделать это автоматически)
5. ✅ Проверьте, что бэкенд запущен и доступен по URL из `VITE_API_URL`
6. ✅ Проверьте CORS настройки в бэкенде (должна быть переменная `FRONTEND_URL` с URL фронтенда)

### Ошибка "Not Found" при обновлении страницы

Если при обновлении страницы на маршруте типа `/dashboard/student` появляется ошибка 404:

1. **Проверьте файл `_redirects`:** Убедитесь, что файл `public/_redirects` существует и содержит:
   ```
   /*    /index.html   200
   ```

2. **Проверьте, что файл копируется в dist:** После сборки (`npm run build`) проверьте, что в папке `dist` есть файл `_redirects`

3. **Альтернатива через Render Dashboard:**
   - Зайдите в настройки Static Site
   - Найдите раздел **"Redirects"** или **"Headers"**
   - Добавьте правило:
     - **From:** `/*`
     - **To:** `/index.html`
     - **Status:** `200`

### Данные не сохраняются

Это нормально для бесплатного плана - данные хранятся в памяти. Для постоянного хранения используйте базу данных.

## 📚 Дополнительные ресурсы

- [Документация Render](https://render.com/docs)
- [Render Environment Variables](https://render.com/docs/environment-variables)

