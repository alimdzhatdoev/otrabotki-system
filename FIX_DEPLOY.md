# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ: Frontend не подключается к Backend

## Проблема

Фронтенд пытается подключиться к `localhost:5000` вместо бэкенда на Render.

**Ошибка в консоли:**
```
localhost:5000/api/auth/login:1 Failed to load resource: net::ERR_CONNECTION_REFUSED
```

## Причина

На фронтенде установлена **неправильная переменная окружения**:
- ❌ Установлена: `FRONTEND_URL` 
- ✅ Нужна: `VITE_API_URL`

## Решение (ШАГ ЗА ШАГОМ)

### Шаг 1: Зайдите в настройки Frontend на Render

1. Откройте Render Dashboard
2. Найдите ваш Static Site (фронтенд)
3. Перейдите в раздел **"Environment"** → **"Environment Variables"**

### Шаг 2: Удалите неправильную переменную

1. Найдите переменную `FRONTEND_URL`
2. Нажмите на иконку корзины (удалить)
3. Подтвердите удаление

### Шаг 3: Добавьте правильную переменную

1. Нажмите **"+ Add Environment Variable"**
2. **KEY:** `VITE_API_URL` (обязательно с префиксом `VITE_`!)
3. **VALUE:** `https://otrabotki-system-back.onrender.com/api`
   - ⚠️ Замените `otrabotki-system-back` на реальное имя вашего бэкенда!
4. Нажмите **"Save Changes"**

### Шаг 4: Пересоберите фронтенд

Render автоматически пересоберет фронтенд после изменения переменных окружения. 

**ВАЖНО:** 
- Дождитесь завершения деплоя (может занять 2-5 минут)
- Проверьте статус в разделе "Events" - должно быть "Deploy successful"
- **Очистите кеш браузера** после деплоя (Ctrl+Shift+R или Cmd+Shift+R)

### Шаг 5: Проверьте, что бэкенд работает

Откройте в браузере:
```
https://otrabotki-system-back.onrender.com/api/health
```

Должен вернуться JSON с `{"status": "ok"}`. Если не работает:
- Бэкенд может "спать" (Free Plan) - подождите 30-60 секунд
- Проверьте логи бэкенда в Render Dashboard

### Шаг 6: Проверьте в браузере

1. Откройте URL фронтенда в браузере
2. **Очистите кеш** (Ctrl+Shift+R)
3. Откройте консоль разработчика (F12) → вкладка "Network"
4. Попробуйте войти
5. В Network посмотрите, на какой URL идут запросы:
   - ✅ Должно быть: `https://otrabotki-system-back.onrender.com/api/auth/login`
   - ❌ НЕ должно быть: `http://localhost:5000/api/auth/login`

## Правильные переменные окружения

### На Frontend (Static Site):
```
VITE_API_URL=https://otrabotki-system-back.onrender.com/api
```

### На Backend (Web Service):
```
FRONTEND_URL=https://otrabotki-system.onrender.com
JWT_SECRET=ваш-секретный-ключ
NODE_ENV=production
PORT=10000
```

## Важно помнить

- **Frontend** использует `VITE_API_URL` для подключения к бэкенду
- **Backend** использует `FRONTEND_URL` для настройки CORS
- Переменные с префиксом `VITE_` доступны только на фронтенде и встраиваются в код при сборке
- После изменения `VITE_API_URL` нужно пересобрать фронтенд!

