# 🚀 Быстрый старт - Инструкция по запуску

## Шаг 1: Установка зависимостей

### 1.1. Установка зависимостей фронтенда

Откройте терминал в корне проекта и выполните:

```bash
npm install
```

### 1.2. Установка зависимостей бэкенда

```bash
cd server
npm install
cd ..
```

## Шаг 2: Настройка переменных окружения

### 2.1. Создайте файл `server/.env`

Создайте файл `server/.env` со следующим содержимым:

```env
PORT=5000
JWT_SECRET=dev-secret-key-change-in-production
NODE_ENV=development
```

**Важно:** В продакшене используйте надёжный секретный ключ для JWT_SECRET!

### 2.2. Создайте файл `.env` в корне проекта

Создайте файл `.env` в корне проекта (рядом с `package.json`):

```env
VITE_API_URL=http://localhost:5000/api
```

## Шаг 3: Запуск проекта

### Вариант А: Запуск в двух терминалах (рекомендуется)

**Терминал 1 - Бэкенд:**
```bash
cd server
npm run dev
```

Вы должны увидеть:
```
🚀 Сервер запущен на http://localhost:5000
📁 Данные хранятся в: ...
🌍 Режим: development
```

**Терминал 2 - Фронтенд:**
```bash
npm run dev
```

Вы должны увидеть:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### Вариант Б: Запуск одной командой (если установлен concurrently)

Если хотите запускать всё одной командой, сначала установите `concurrently`:

```bash
npm install --save-dev concurrently
```

Затем добавьте в корневой `package.json` скрипт:

```json
"scripts": {
  "dev": "vite",
  "dev:server": "cd server && npm run dev",
  "dev:full": "concurrently \"npm run dev:server\" \"npm run dev\"",
  "build": "vite build",
  "preview": "vite preview"
}
```

И запускайте:

```bash
npm run dev:full
```

## Шаг 4: Проверка работы

1. Откройте браузер и перейдите на `http://localhost:5173`
2. Вы должны увидеть страницу входа
3. Попробуйте войти с демо-аккаунтом:
   - **Логин:** `ivanov`
   - **Пароль:** `123`

## 🔐 Демо-аккаунты

| Роль | Логин | Пароль |
|------|-------|--------|
| 🎓 Студент | `ivanov` | `123` |
| 👩‍🏫 Преподаватель | `petrova` | `123` |
| 🏫 Оператор | `operator` | `123` |
| ⚙️ Администратор | `admin` | `123` |

## ⚠️ Возможные проблемы

### Проблема: "Cannot find module" при запуске бэкенда

**Решение:** Убедитесь, что вы установили зависимости:
```bash
cd server
npm install
```

### Проблема: "Port 5000 is already in use"

**Решение:** Измените порт в `server/.env`:
```env
PORT=5001
```

И обновите `VITE_API_URL` в корневом `.env`:
```env
VITE_API_URL=http://localhost:5001/api
```

### Проблема: "Failed to fetch" на фронтенде

**Решение:** 
1. Убедитесь, что бэкенд запущен (проверьте терминал 1)
2. Проверьте, что `VITE_API_URL` в `.env` правильный
3. Перезапустите фронтенд после изменения `.env`

### Проблема: "ENOENT: no such file or directory" в server/data

**Решение:** Убедитесь, что все JSON-файлы созданы в `server/data/`:
- `users.json`
- `courses.json`
- `slots.json`
- `teacherSchedules.json`
- `limits.json`
- `attendance.json`

Если файлов нет, они создадутся автоматически при первом запуске.

## 📝 Структура команд

### Бэкенд (в папке `server/`)
- `npm run dev` - запуск в режиме разработки с автоперезагрузкой
- `npm start` - запуск в продакшн режиме

### Фронтенд (в корне проекта)
- `npm run dev` - запуск в режиме разработки
- `npm run build` - сборка для продакшена
- `npm run preview` - предпросмотр собранного проекта

## 🎯 Что дальше?

1. Войдите как **оператор** (`operator` / `123`)
2. Создайте расписание преподавателя
3. Система автоматически создаст слоты на 4 недели вперёд
4. Войдите как **студент** и запишитесь на слот
5. Войдите как **преподаватель** и отметьте посещаемость
6. Войдите как **администратор** и посмотрите аналитику

## 💡 Полезные советы

- Бэкенд должен быть запущен **до** запуска фронтенда
- При изменении `.env` файлов нужно перезапустить соответствующий сервис
- Все данные хранятся в `server/data/*.json` - можно редактировать вручную
- JWT токены действительны 7 дней

---

**Готово!** 🎉 Теперь ваша система управления отработками работает с полноценным бэкендом!









