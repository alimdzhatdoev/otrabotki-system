# Система Отработки 2.0

Полнофункциональная система управления отработками для медицинского института с авторизацией, календарём и аналитикой.

## 🚀 Технологии

**Frontend:**
- **React** (JavaScript, функциональные компоненты)
- **react-router-dom v6** - навигация и защищённые маршруты
- **CSS Modules** + **BEM** - стилизация
- **Context API** - управление авторизацией
- **Vite** - быстрая сборка

**Backend:**
- **Node.js** + **Express** - REST API сервер
- **JSON-файлы** - хранение данных (server/data/*.json)
- **JWT** - авторизация
- **CORS** - для работы с фронтендом

## 📦 Установка и запуск

### 1. Установка зависимостей

```bash
# Установка зависимостей фронтенда
npm install

# Установка зависимостей бэкенда
cd server
npm install
cd ..
```

### 2. Настройка переменных окружения

Создайте файл `server/.env`:
```env
PORT=5000
JWT_SECRET=your-secret-key-change-in-production
NODE_ENV=development
```

Создайте файл `.env` в корне проекта:
```env
VITE_API_URL=http://localhost:5000/api
```

### 3. Запуск проекта

**Вариант 1: Запуск отдельно**

```bash
# Терминал 1: Запуск бэкенда
cd server
npm run dev

# Терминал 2: Запуск фронтенда
npm run dev
```

**Вариант 2: Одной командой (если установлен concurrently)**

```bash
npm run dev:full
```

Бэкенд будет доступен на `http://localhost:5000`
Фронтенд будет доступен на `http://localhost:5173`

### 4. Сборка для продакшена

```bash
# Сборка фронтенда
npm run build

# Запуск бэкенда в продакшене
cd server
npm start
```

## 🔐 Авторизация

Вход по логину и паролю. Роль определяется автоматически. Нельзя переключиться на другую роль без выхода.

### Демо-аккаунты (пароль `123` для всех)

| Роль | Логин | Описание |
|------|-------|----------|
| 🎓 Студент | `ivanov` | Иванов Иван, группа 201, 1 курс |
| 👩‍🏫 Преподаватель | `petrova` | Петрова И.С., Анатомия/Гистология |
| 🏫 Оператор | `operator` | Оператор деканата |
| ⚙️ Администратор | `admin` | Админ системы |

## 👥 Роли и функционал

### 🎓 Студент

**Визуальный календарь с умным дизайном:**
- ✅ Дни **с отработками выделены цветом** (яркие)
- ✅ Остальные дни **полупрозрачные/серые**
- ✅ Клик на день → список слотов справа
- ✅ **Кнопка "Записаться"** на каждом слоте
- ✅ Автоматическая валидация лимитов (1/день, 3/неделю)
- ✅ Фильтр по предметам
- ✅ Раздел "Мои записи" с возможностью отмены

**Как работает:**
1. Открыть календарь
2. Видеть яркие дни с доступными слотами
3. Кликнуть на день → просмотр слотов
4. Нажать "Записаться" → подтверждение (с проверкой лимитов)

### 🏫 Оператор (Деканат)

**Новая логика — управление расписаниями:**
- ✅ **Добавление расписаний** преподавателей (не создание слотов вручную!)
- ✅ Указываются: преподаватель, предмет, день недели, время, макс. студентов
- ✅ **Слоты генерируются автоматически** на 4 недели вперёд
- ✅ Таблица всех расписаний
- ✅ Просмотр созданных слотов по расписанию
- ✅ Удаление расписаний
- ✅ Статистика: расписаний, слотов, записей

**Как работает:**
1. Нажать "+ Добавить расписание преподавателя"
2. Выбрать преподавателя, курс, предмет
3. Указать день недели (например, "Понедельник")
4. Указать время и количество мест
5. Система **автоматически создаёт слоты** на все понедельники следующих 4 недель

**Пример:** "Петрова И.С., Анатомия, Понедельник 10:00-11:30, макс 2 студента"
→ Система создаст слоты на все ближайшие 4 понедельника

### 👩‍🏫 Преподаватель

**Список отработок (по умолчанию):**
- ✅ **Список по умолчанию** (не календарь)
- ✅ **Сортировка от сегодняшней даты** вперёд
- ✅ Отображение **кто записан** на каждую отработку
- ✅ Два чекбокса: **"Пришёл"** и **"Отработал"**
- ✅ Статистика: слотов, студентов, пришло, отработали
- ✅ Календарь доступен вторым режимом (переключатель "Список/Календарь")
- ✅ Фильтры по предмету, курсу

**Как работает:**
1. Открыть список (по умолчанию)
2. Видеть отработки от сегодня и дальше
3. Раскрыть слот → список записанных студентов
4. Отметить чекбокс "Пришёл" (присутствие)
5. Отметить чекбокс "Отработал" (выполнил работу)
6. Данные сохраняются автоматически

### ⚙️ Администратор

**Мощная аналитика и управление:**

**5 вкладок:**
1. **📊 Аналитика** (по умолчанию)
   - Общая статистика (слотов, заявок, % посещаемости, % отработки)
   - Аналитика по преподавателям (слотов, заявок, пришло, отработали, %)
   - Аналитика по предметам (заявок, отработали, прогресс-бар, %)

2. **👩‍🏫 Преподаватели**
   - Список всех преподавателей
   - ФИО, логин, предметы
   - **Кнопка экспорта в CSV**

3. **📝 Заявки**
   - Список всех записей студентов
   - Студент, группа, предмет, дата, время, преподаватель
   - Статусы: пришёл ✓/—, отработал ✓/—
   - **Кнопка экспорта в CSV**

4. **🎓 Студенты**
   - Список всех студентов
   - ФИО, логин, группа, курс, количество заявок
   - **Кнопка экспорта в CSV**

5. **⚙️ Настройки**
   - Изменение лимитов
   - Управление предметами по курсам

**Экспорт в CSV:**
- Все данные можно выгрузить в CSV (открывается в Excel)
- Поддержка кириллицы (UTF-8 BOM)
- Кнопки экспорта на вкладках Преподаватели/Заявки/Студенты

## 📅 Календарь

Универсальный компонент календаря:
- **Для студента:** дни с слотами яркие, остальные полупрозрачные
- **Для оператора/преподавателя:** показывает все слоты
- Навигация по месяцам
- Индикаторы (точки) на днях с отработками
- Клик на день → панель слотов справа
- Кнопка "Записаться" на каждом слоте (для студента)

## 🎨 Дизайн

Тёмная тема в стиле modern dashboard:
- **Background:** `#0F1424`
- **Surface:** `#141A2F`
- **Primary:** `#5B5FFF`
- **Success:** `#38BDF8`
- **Text:** `#FFFFFF` / `#A5B4FC`
- **Border-radius:** 12-20px
- **Отступы:** 16-32px

## 📁 Структура проекта

```
otrabotki-system/
├── server/                      # Backend (Node.js/Express)
│   ├── index.js                 # Точка входа сервера
│   ├── config/
│   │   └── config.js            # Конфигурация
│   ├── data/                    # JSON-файлы с данными
│   │   ├── users.json
│   │   ├── courses.json
│   │   ├── slots.json
│   │   ├── teacherSchedules.json
│   │   ├── limits.json
│   │   └── attendance.json
│   ├── routes/                  # Роуты API
│   │   ├── authRoutes.js
│   │   ├── studentRoutes.js
│   │   ├── teacherRoutes.js
│   │   ├── operatorRoutes.js
│   │   └── adminRoutes.js
│   ├── controllers/             # Контроллеры
│   ├── services/                # Бизнес-логика
│   │   ├── fileDb.js
│   │   ├── slotService.js
│   │   ├── limitService.js
│   │   └── analyticsService.js
│   ├── middleware/              # Middleware
│   │   ├── authMiddleware.js
│   │   └── errorHandler.js
│   └── utils/                  # Утилиты
│       ├── idGenerator.js
│       └── dateUtils.js
│
└── src/                         # Frontend (React)
    ├── api/                     # API-клиент
    │   ├── httpClient.js
    │   ├── authApi.js
    │   ├── studentApi.js
    │   ├── teacherApi.js
    │   ├── operatorApi.js
    │   └── adminApi.js
    ├── context/
    │   └── AuthContext.jsx      # Управление авторизацией
    ├── layout/
    │   ├── MainLayout.jsx
    │   ├── Sidebar.jsx
    │   └── Topbar.jsx
    ├── pages/
    │   ├── LoginPage.jsx
    │   ├── StudentDashboard.jsx
    │   ├── TeacherDashboard.jsx
    │   ├── OperatorSchedule.jsx
    │   └── AdminSettings.jsx
    └── components/
        └── Calendar.jsx
```

## 💾 Хранилище данных

**Backend (JSON-файлы в `server/data/`):**
- `users.json` - пользователи системы
- `courses.json` - курсы и предметы
- `slots.json` - слоты отработок
- `teacherSchedules.json` - расписания преподавателей
- `limits.json` - лимиты записей (день/неделя)
- `attendance.json` - посещаемость с полями: `attended`, `completed`

**Frontend (localStorage):**
- `authToken` - JWT токен авторизации

## ✨ Ключевые изменения v2.0

### Студент
- ✅ Дни с отработками **выделены ярким цветом**
- ✅ Остальные дни **полупрозрачные**
- ✅ **Кнопка "Записаться"** вместо клика на весь слот

### Преподаватель
- ✅ **Список по умолчанию** (не календарь)
- ✅ **Сортировка от сегодня** вперёд
- ✅ **Два чекбокса:** "Пришёл" и "Отработал"
- ✅ Видно **кто записан** на каждую отработку

### Оператор
- ✅ **Полностью новая логика!**
- ✅ Добавление **расписаний** преподавателей
- ✅ **Автоматическое создание слотов** на 4 недели
- ✅ Не нужно создавать слоты вручную

### Администратор
- ✅ **5 вкладок:** Аналитика, Преподаватели, Заявки, Студенты, Настройки
- ✅ **Подробная аналитика:** по преподавателям, предметам, статистика
- ✅ **Экспорт в CSV/Excel** всех данных
- ✅ Прогресс-бары, проценты, визуализация

## 🎯 Основные сценарии использования

### Студент записывается на отработку
1. Войти как `ivanov` / `123`
2. Видеть календарь, где **яркие дни = есть отработки**
3. Кликнуть на яркий день
4. В правой панели появятся слоты
5. Нажать кнопку **"Записаться"**
6. Система проверит лимиты и запишет

### Оператор создаёт расписание
1. Войти как `operator` / `123`
2. Нажать **"+ Добавить расписание преподавателя"**
3. Выбрать преподавателя (например, Петрова И.С.)
4. Выбрать предмет (Анатомия), курс (1 курс)
5. Выбрать день недели (Понедельник)
6. Указать время (10:00 - 11:30) и количество мест (2)
7. Нажать "Создать расписание"
8. **Система автоматически создаст слоты на все понедельники следующих 4 недель**

### Преподаватель отмечает работу
1. Войти как `petrova` / `123`
2. Видеть **список** отработок (от сегодня вперёд)
3. Раскрыть отработку (кнопка ▼)
4. Видеть **список студентов**, которые записались
5. Отметить чекбокс **"Пришёл"** для присутствующих
6. Отметить чекбокс **"Отработал"** для выполнивших работу
7. Данные сохраняются автоматически

### Админ смотрит аналитику и экспортирует
1. Войти как `admin` / `123`
2. По умолчанию открыта вкладка **"📊 Аналитика"**
3. Видеть:
   - Общую статистику (слотов, заявок, % посещаемости, % отработки)
   - Таблицу по преподавателям (у кого сколько слотов, заявок, % выполнения)
   - Карточки по предметам (с прогресс-барами)
4. Перейти на вкладку **"📝 Заявки"**
5. Нажать **"📥 Экспорт в CSV"**
6. Файл сохранится и откроется в Excel

## 🔧 Особенности реализации

### Автогенерация слотов
- Оператор создаёт **шаблон расписания** (день недели + время)
- `useEffect` в `OperatorSchedule` генерирует слоты на 4 недели вперёд
- Проверяется, что слот не создан ранее (по дате + времени + преподавателю)
- Слоты добавляются в `localStorage`

### Структура attendance
```javascript
{
  slotId: 's1',
  studentId: 'u1',
  attended: true,    // Пришёл
  completed: false   // Отработал
}
```

### Экспорт в CSV
- Функция `exportToCSV` создаёт CSV с BOM для кириллицы
- Заголовки берутся из ключей объектов
- Данные экранируются кавычками
- `Blob` + `URL.createObjectURL` для скачивания

## 📊 Аналитика

Админ видит:
- **Общую статистику:** слотов, заявок, % посещаемости, % отработки
- **По преподавателям:** кол-во слотов, заявок, пришло, отработали, % выполнения
- **По предметам:** кол-во заявок, кол-во отработавших, прогресс-бар, %

Все данные в реальном времени из `localStorage`.

## 🔌 API Endpoints

### Авторизация
- `POST /api/auth/login` - вход в систему
- `GET /api/auth/me` - получение текущего пользователя

### Студент
- `GET /api/student/slots` - доступные слоты
- `POST /api/student/book` - запись на слот
- `DELETE /api/student/book/:slotId` - отмена записи
- `GET /api/student/bookings` - мои записи
- `GET /api/student/limits` - информация о лимитах

### Преподаватель
- `GET /api/teacher/slots` - мои слоты
- `GET /api/teacher/slots/:slotId/students` - студенты слота
- `PATCH /api/teacher/attendance` - обновление посещаемости
- `GET /api/teacher/stats` - статистика

### Оператор
- `GET /api/operator/teacher-schedules` - все расписания
- `POST /api/operator/teacher-schedules` - создать расписание
- `DELETE /api/operator/teacher-schedules/:id` - удалить расписание
- `POST /api/operator/generate-slots` - сгенерировать слоты
- `GET /api/operator/slots` - все слоты
- `POST /api/operator/teachers` - создать преподавателя

### Администратор
- `GET /api/admin/analytics` - аналитика
- `GET /api/admin/users` - список пользователей
- `POST /api/admin/users` - создать пользователя
- `PATCH /api/admin/users/:id` - обновить пользователя
- `DELETE /api/admin/users/:id` - удалить пользователя
- `GET /api/admin/limits` - получить лимиты
- `PATCH /api/admin/limits` - обновить лимиты
- `GET /api/admin/courses` - список курсов
- `POST /api/admin/courses` - создать курс
- `GET /api/admin/requests` - все заявки

## 📝 Примечания

- Пароли в открытом виде (только для демо!)
- Данные хранятся в JSON-файлах на бэкенде
- JWT токены действительны 7 дней
- Слоты генерируются при создании/изменении расписаний
- Экспорт работает только в современных браузерах
- Для работы требуется запущенный бэкенд-сервер

## 🚦 Быстрый старт

```bash
npm install
npm run dev
# Откроется http://localhost:5173
```

Кликните на любую демо-карточку на странице входа → автозаполнение → "Войти"

Готово! 🎉
