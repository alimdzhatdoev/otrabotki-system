# 🔍 Диагностика проблемы подключения Frontend → Backend

## Быстрая проверка

### 1. Проверьте переменные окружения на Frontend

Зайдите в Render Dashboard → ваш Static Site → Environment Variables

**Должно быть:**
```
VITE_API_URL=https://otrabotki-system-back.onrender.com/api
```

**НЕ должно быть:**
- ❌ `FRONTEND_URL` (эта переменная только для бэкенда!)

### 2. Проверьте, что фронтенд пересобрался

После изменения `VITE_API_URL`:
- Render автоматически запускает пересборку
- Дождитесь завершения деплоя (может занять 2-5 минут)
- Проверьте статус в разделе "Events" или "Logs"

### 3. Проверьте, что бэкенд работает

Откройте в браузере:
```
https://otrabotki-system-back.onrender.com/api/health
```

**Должен вернуться JSON:**
```json
{
  "status": "ok",
  "timestamp": "2024-..."
}
```

Если не работает:
- Бэкенд "спит" (Free Plan) - подождите 30-60 секунд
- Проверьте логи бэкенда в Render Dashboard
- Убедитесь, что бэкенд запущен (зеленый статус)

### 4. Проверьте URL в собранном фронтенде

1. Откройте фронтенд в браузере
2. Нажмите F12 → вкладка "Network"
3. Попробуйте войти
4. Посмотрите, на какой URL идут запросы

**Должно быть:**
```
https://otrabotki-system-back.onrender.com/api/auth/login
```

**НЕ должно быть:**
```
http://localhost:5000/api/auth/login
```

### 5. Проверьте переменные окружения на Backend

Зайдите в Render Dashboard → ваш Web Service → Environment Variables

**Должно быть:**
```
FRONTEND_URL=https://otrabotki-system.onrender.com
JWT_SECRET=ваш-секретный-ключ
NODE_ENV=production
PORT=10000
```

### 6. Проверьте CORS

Если запросы идут на правильный URL, но все равно ошибка:
- Проверьте консоль браузера (F12) на наличие CORS ошибок
- Убедитесь, что `FRONTEND_URL` на бэкенде точно совпадает с URL фронтенда (включая `https://`)

## Частые ошибки

### Ошибка: "localhost:5000" в запросах

**Причина:** `VITE_API_URL` не установлена или фронтенд не пересобрался

**Решение:**
1. Установите `VITE_API_URL` на фронтенде
2. Дождитесь завершения пересборки
3. Очистите кеш браузера (Ctrl+Shift+R)

### Ошибка: CORS policy

**Причина:** `FRONTEND_URL` на бэкенде не совпадает с реальным URL фронтенда

**Решение:**
1. Проверьте точное совпадение URL (включая `https://`, без слеша в конце)
2. Перезапустите бэкенд после изменения `FRONTEND_URL`

### Ошибка: "Failed to fetch" или "Network error"

**Причина:** Бэкенд недоступен или "спит"

**Решение:**
1. Проверьте, что бэкенд запущен (зеленый статус в Render)
2. Подождите 30-60 секунд (cold start на Free Plan)
3. Проверьте логи бэкенда на наличие ошибок

## Пошаговая инструкция исправления

### Шаг 1: Убедитесь, что бэкенд работает

```bash
# Откройте в браузере:
https://otrabotki-system-back.onrender.com/api/health
```

Если не работает - проверьте логи бэкенда.

### Шаг 2: Исправьте переменные на Frontend

1. Render Dashboard → Static Site → Environment
2. Удалите `FRONTEND_URL` (если есть)
3. Добавьте `VITE_API_URL` = `https://otrabotki-system-back.onrender.com/api`
4. Сохраните

### Шаг 3: Дождитесь пересборки

- Проверьте статус в "Events"
- Дождитесь "Deploy successful"

### Шаг 4: Очистите кеш браузера

- Нажмите Ctrl+Shift+R (Windows/Linux)
- Или Cmd+Shift+R (Mac)

### Шаг 5: Проверьте в консоли браузера

1. F12 → Console
2. Попробуйте войти
3. Проверьте, на какой URL идут запросы

## Если ничего не помогло

1. **Проверьте логи бэкенда** в Render Dashboard
2. **Проверьте логи сборки фронтенда** в Render Dashboard
3. **Проверьте консоль браузера** (F12) на наличие других ошибок
4. **Попробуйте открыть API напрямую** в браузере:
   ```
   https://otrabotki-system-back.onrender.com/api/health
   ```


